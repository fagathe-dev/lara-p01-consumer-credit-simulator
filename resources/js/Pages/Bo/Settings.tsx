import React from 'react';
import { BoShell } from '@/components/Bo/BoShell';

const Settings: React.FC = () => (
  <BoShell
    title="Paramètres"
    description="Configuration du back-office (squelette)."
  />
);

export default Settings;
